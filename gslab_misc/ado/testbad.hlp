.-
help for ^testbad^
.-

Syntax
-----------------------------------------------------------------------

	testbad command, [showoutput]

Description
-----------------------------------------------------------------------

Unit test utility which confirms that the command entered fails.

Options
-----------------------------------------------------------------------

showoutput: Run command noisily, printing all output / error messages.
    Note that command itself may have options. In this case, showoutput
    can be entered after the last option to command. You should not
    enter a second comma in this case.

Examples
-----------------------------------------------------------------------

    testbad regruss y x
    testbad regress y x, bagel showoutput
    testbad gen z==2, showoutput
    testbad replace y = 0 if x<<0

See also
--------
@testgood@

